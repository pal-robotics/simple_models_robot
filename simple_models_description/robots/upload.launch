<?xml version="1.0" encoding="UTF-8"?>
<launch>
    <arg name="robot" default="" />

    <!-- urdf to param server -->
    <param
        name="robot_description"
        command="$(find xacro)/xacro --inorder '$(find simple_models_description)/urdf/$(arg robot).urdf'" />
</launch>
